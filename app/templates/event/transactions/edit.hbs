{{#if (is-equal form.formType "transfer")}}
  {{transfer-form transfer=form onDelete=(route-action "delete") title=(t "transferForm.viewTitle")}}
{{else}}
  {{transaction-form transaction=form users=users currencies=currencies onDelete=(route-action "delete") onModelUpdated=(route-action "modelUpdated") title=(t "transactionForm.editTitle")}}
{{/if}}
